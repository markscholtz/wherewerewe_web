- @user.series.each_with_index do |s, i|
  %div
    %h1= s.name
    %p= s.overview
    -#TODO: The ruby code inside of the conditional if is being executed regardless of
    -# whether the conditional is evaluating to true or false
    - if s.viewed? @user.id
      %p= Viewing.last_viewed(user_id: @user.id, series_id: @user.series[i].id).first.episode.name

